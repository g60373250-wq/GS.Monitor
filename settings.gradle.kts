pluginManagement {
    repositories {
        google()
        mavenCentral()
        maven("https://jetbrains.space")
    }
}

dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
        maven("https://jetbrains.space")
    }
}

rootProject.name = "GS.Monitor"
